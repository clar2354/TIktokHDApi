services:
  - type: web
    name: tiktok-api
    env: python
    plan: free
    branch: main
    runtime: python
    buildCommand: ""
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    autoDeploy: true
